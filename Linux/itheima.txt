通用格式（[]表示可选）：
command [-options] [parameter]
command:命令本身
-options:可选选项
parameter:可选参数

ls以平埔的方式展示当前的工作目录（默认home）:
-a:表示all的意思，即列出所有文件（包含隐藏的文件/文件夹）
-l表示以列表（竖向排列）的形式展示内容，并展示更多信息
-h表示以易于阅读的方式，列出文件的大小（带上单位）
（一起使用可以写成ls -alh）
ls [-a -l -h] [路径]

cd切换工作目录(默认为home)：
cd [路径]

pwd展示当前工作目录命令：
pwd

相对路径和绝对路径：
绝对路径：以根目录为起点
相对路径：以当前目录为起点

特殊路径符：
.   :表示当前目录
..  :表示上一级目录
~ :表示home目录

通过mkdir创建文件夹：
来自英文：Make Directory
路径为必选（相对和绝对都可以，特殊路径父也可以）
-p：表示自动创建不存在的父目录，适用于创建连续多层级的目录
mkdir [-p] 路径

touch创建文件：
无选项，参数必填，相对绝对特殊路径符都能使用
touch 路径

cat查看文件内容：
cat 路径

more查看文件内容：
more支持翻页，内容过多可以一页一页展示
more 路径

cp命令复制文件/文件夹，来自copy：
cp [-r] 路径1 路径2
-r在复制文件夹的时候用，表示递归（一次创建多个文件夹）
路径一表示被复制的地方，路径二表示要复制去的地方

mv命令移动文件/文件夹，来自move：
路径1表示被移动的文件或文件夹
路径2表示要移动去的地方，如果文件不存在，则进行改名
mv 路径1 路径2

rm用于删除文件、文件夹，来自remove：
-r 用于删除文件夹
-f 表示强制删除（force）
参数1.......参数表示要删除的文件或文件夹的路径，空格隔开

执行的命令本质是一个程序，可以找到他的本体

可以通过which命令查找命令的程序文件放在哪里：
语法：which  要查找的命令

通过find搜索指定文件：
按文件名查找：
语法：find 启示路径 -name “被查找文件名”
按文件大小查找：
语法：find 启示路径 -size +|-n[kmg]
+、-表示大于、小于
n表示大小数据
kmg表示大小单位，k(小写字母)表示kb、M表示MB、G表示GB

*表示通配符，做模糊匹配：
test*，表示匹配任何以test开头的内容
*test，表示任何以test结尾的内容
*test*，表示任何包含test的内容

通过grep命令，从文件中通过关键字过滤文件行（就是有关键字的就展示出来）：
语法：grep [-n] “关键字” 文件路径
-n：表示在结果中显示匹配的行的行号
“关键字”：必填，表示过滤的关键字，带有空格或特殊符号
文件路径：必填，表示要过滤内容的文件路径，可作为内容输入端口

可以通过wc命令统计文件的行数、单词数量等：
语法：wc  [-l -w -c -m] 文件路径
-c,统计bytes数量
-m,统计字符数量
-l,统计行数
-w,统计单词数量（按照空格划分）
文件路径，被统计的文件，可作为内容输入端口

管道符：（|）将左边命令的结果作为右边命令的输入
可以嵌套使用

可以用echo在命令行输出指定内容：
语法：echo “输出内容”
反引号  ：`   ` ，被包围的内容将不会作为输出，而是当成命令去执行
重定向符：> ，将左侧命令的结果，覆盖写到符号右侧指定文件中
重定向符：>>，将左侧命令的结果，追加写入到符号右侧指定文件中

使用tail命令，可以查看文件尾部内容，跟踪文件的最新更改：
语法：tail [-f -num] 路径
-f，表示持续跟踪
-num，表示查看尾部多少行，不填默认10行

vi/vim文本编辑器（任何情况下按esc都能回到命令模式）：
命令模式（Command mode）：敲的按键都被识别为命令，不能进行文本自由编辑    （刚进入默认为这个）
输入模式（Insert mode）：即编辑模式、插入模式可以对文件内容进行自由编辑   （命令模式下按iao其中一个进入，按esc退回到命令模式）
底线命令模式（Last line mode）：通常用于文件的保存、退出  （命令模式按：进入，按wq保存并退出vim	）

命令模式快捷键：
i		在当前光标位置进入输入模式
a 		在当前光标位置之后进入输入模式
o		在当前光标下一行进入输入模式
I		在当前行的开头进入输入模式
A		在当前行的结尾进入输入模式
O		在当前光标的上一行进入输入模式
0		移动光标到当前行的开头
$		移动到光标当前行的结尾
pageup(pgup)		向上翻页
pangdown(pgdn)		向下翻页
/		进入搜索模式
n		向下继续搜索
N		向上继续搜索
键盘上、下、左、右对应有k、j、h、l移动光标
dd		删除光标行所在的内容
ndd		删除当前光标向下n行
yy		复制当前行
nyy		复制当前行和下面n行
p		粘贴复制内容
u		撤销修改
ctrl+r	反向撤销的修改
gg		跳到首行
G		跳到尾行
dG		当前行开始向下全部删除
dgg		当前行开始向上全部删除
d$		从当前开始删除到本行的结尾
d0		从当前开始删除到本行的开头

底线命令模式快捷键：
：wq		保存并退出
：q			仅退出
：q！		强制退出
：w			仅保存
：set  nu		显示行号
：set  paste	设置粘贴模式（从外部复制东西时使用）

语法：
vi 文件路径
vim 文件路径
（如果文件路径不存在，则用于编辑新文件，如果文件存在，则用于编辑已有文件）

su命令切换用户（来自：switch user）：
语法：su [-] [用户名]
-表示是否在切换后加载环境变量
用户名省略时默认为root
切换后可通过exit命令或ctrl+d返回上一个用户
root切换到其他无需密码，其他用户切换到root则需要密码

为普通用户配置sudo认证就可以临时拥有root权限
只需要在命令前加上sudo就可以让命令临时以root身份运行

配置方法：在root下，执行visudo，会自动通过vi编译器打开：/etc/sudoers
在文件最后添加：itheima ALL=(ALL)		NOPASSWD:  ALL

创建用户组
groupadd	用户组名
删除用户组
groupdel	用户组名

创建用户
useradd [-g -d] 用户名
-g：指定用户的组，不指定-g，会自动创建同名组并自动加入，指定-g需要组已存在，如已存在同名组则必须使用-g
-d：-d指定用户的home目录，不指定时默认在 /home/用户名

删除用户
userdel [-r] 用户名
-r：删除用户的home目录，不使用-r，删除用户时home保留

查看用户所属组
id [用户名]
不提供用户名时查看自身

修改用户所属组
usermod -aG 用户组 用户名
将指定用户加入指定用户组

查看当前系统中有哪些用户
getent passwd
共有七份信息：
用户名：密码（x）：用户id：组id：描述信息（无用）：home目录：执行终端（默认bash）

查看系统中有哪些组
getent group
共有三份信息：
组名称：组认证（x）：组id

权限：rwx，可读、可写、可执行（针对文件和文件夹有细微不同）

修改权限信息（只有文件/文件夹的所属用户或者root用户可以修改）
chmod [-R] 权限 文件/文件夹
-R：对文件内的全部内容应用相同操作
示例：chmod -R u=rwx，g=rx，o=x test
u表示user所属用户权限，g表示group组权限，o表示other其他用户权限

修改文件、文件夹所属用户和用户组（此命令只能root用户执行）
chown [-R] [用户][:][用户组] 文件或文件夹
-R同上
：是用来分隔用户和用户组的，所以可以只修改用户或者用户组

clrl+d退出或登出用户登录，或者推出某些特定程序（不能退出vi/vim）

history查看历史输入过的命令

！+命令关键开头，自动找到以前输入过的命令并执行（比如：！py=python）

CTRL+r匹配以前输入过的命令的关键字

CTRL+a			跳到命令开头
CTRL+e			跳到结尾
CTRL+键盘左键	向左跳一个单词
CTRL+键盘右键	向右跳一个单词

CTRL+l或者clear清空终端内容


yum：rpm包软件管理器（相当应用商店），用于自动化安装配置Linux软件，并可以解决自动依赖问题
yum [-y] [install | remove | search] 软件名称
-y自动确认，无需手动确认安装或卸载过程
install：安装  remove：卸载  search：搜索

centos：.rpm,yum
ubuntu:.deb,apt

systemctl命令启动、停止、开机自启软件（服务）
systemctl start | stop | status | enable | disable 服务名
start启动；stop关闭；status查看状态；enale开启开机自启；disable关闭开机自启
系统内置服务有：
networkmanager，主网络服务
network，副网络服务
firewalld，防火墙服务
sshd，ssh服务

Linux的软连接可以将文件、文件夹链接到其他位置（类似window的）
ln -s 参数1 参数2
-s：创建软连接
参数1：被链接的文件或文件夹
参数2：要链接去的目的地

date命令查看系统中的时间
date [-d] [+格式化字符串]
-d按照给定的字符串显示日期,一般用于日期计算
(在后面用双引号"+ n year/month/day/hour/minute/second"也可以用格式化字符串代替)

格式化字符串：通过特定的字符串标记，来显示的日期格式
%Y 年；%y年份后两位数字（00..99）；%m月份（01..12）
%d日（01..31）；%H小时（00..23）；%M分钟（00..59）
%S 秒（00..60）；%s自1970-01-01 00:00:00 UTC 到现在的秒数

修改Linux时区（需要root）
将系统自带的localtime删除，并将/usr/share/zoneinfo/Asia/Shanghai文件链接为localtime文件

可以通过ntp程序自动校准系统时间（安装ntp并开启开机自启）
手动校准（需要root）：ntpdate -u ntp.aliyun.com

ifconfig查看ip地址，如果无法使用，用yum安装一个

hostname查看主机名

修改主机名（需要root，重新登陆就能看到）
hostnamectl set-hostname 主机名

DNS（域名）解析：
IP地址是主机的唯一标识，但是太长，所以有了域名（比如：baidu.com），只要建立映射就可以只记住域名就行
解析过程：
先查看本地的记录（私人地址本）
window看	C\Window\System32\drivers\etc\hosts
lLinux看		/etc/hosts
再联网去DNS服务器（114.114.114.114或者8.8.8.8等）

ping命令检查指定的网络服务是否为可联通状态
ping [-c num] ip或主机名
-c,检查的次数，不使用-c将无限次检查

wget是非交互式的文件下载器，可以在命令行内下载网络文件
wget [-b] url
-b,可选，后台下载，会将日志写入到当前工作目录的wegt-log文件
url，下载链接

可以通过tail -f wget-log监控后台下载进度

curl可以发送http网络请求，可用于：下载文件、获取信息等
curl [-0] url
-0,用于下载文件，当url是下载链接时，可以使用这个选项

nmap查看设备被占用的端口（需要先安装nmap程序）
nmap 被查看的IP（127.0.0.1是自己）

通过netstat命令查看指定端口的占用情况（需要安装）
netstat -anp  | grep 端口号

查看系统进程信息
ps [-e -f]
-e，显示全部的进程
-f，以完全格式化的形式展示信息
UID：进程所属的用户ID
PID：进程的进程号ID
PPID：进程的父ID（启动此进程在其他进程里）
C：进程的CPU占用率（百分比）
STIME：进程的启动时间
TTY：启动此进程的终端序号，如：？，表示非终端启动
TIME：进程占用CPU的时间
CMD：进程对应的名称或启动路径或启动命令

进程关闭
kill [-9] 进程ID
-9，表示强制关闭。不使用此选项会向进程发出信号要求其关闭，但是否关闭取决于进程本身的处理机制

通过top查看CPU、内存使用情况，类似Windows的任务管理器
默认每隔5秒刷新一次，按q或者CTRL+C退出
行列解读：
PID：进程ID
USER：进程所属用户
PR：进程优先级，越小越高
NI：负值表示高优先级，正值表示低优先级
VIRT:进程使用虚拟内存，单位KB
RES:进程使用物理内存，单位KB
SHR:进程使用共享内存，单位KB
S:进程状态（S休眠，R运行，Z僵死状态，N负数优先级，！空闲状态）
%CPU:进程占用CPU率
%MEM：进程占用内存率
TIME+:进程使用CPU时间总计，单位10毫秒
COMMAND:进程命令或名称或程序文件路径
可选项：
-p	只显示某个进程的信息
-d	设置刷新时间，默认是5秒
-c	显示产生进程的完整命令，默认是进程名
-n	指定刷新次数，比如：top -n 3，刷新输出3次后退出
-b	以非交互式全屏模式运行，以批次的方式执行top，一般配合-n指定输出几次统计信息，将输出重定向到指定文件，比如top -b -n 3 > /tmp/top.tmp
-i	不显示任何闲置（idle）或无用（zombie）的进程
-u	查找特定用户启动的进程
top交互式选项（非-b选项启动）：
按h键，会帮助显示画面
按c键，相当于-c，再次按c，恢复默认
按f键，可以选择需要展示的项目
按M键，根据驻留内存大小（RES）排序
按P键，根据CPU使用百分比大小进行排序
按T键，根据时间/累计时间进行排序
按E键，切换顶部内存显示单位
按e键，切换进程内存显示单位
按L键，切换显示平均负载和启动时间信息
按i键，等同-i，再次按下变回默认
按t键，切换显示CPU状态信息
按m键，切换显示内存信息

df命令查看硬盘的使用情况
df [-h]
-h,以更加人性化的单位显示

iostat查看CPU、磁盘的相关信息
iostat [-x] [num1] [num2]
-x，显示更多信息
num1，刷新间隔
num2，刷新几次
内容解读：
tps：该设备每秒的传输次数。“一次传输次数”意思是“一次I/O请求”多个逻辑请求可能会被合并为“一次I/O请求”，“一次传输”请求大小是未知的
-x的内容解读：
rKB/s：每秒发送到设备的读取请求数
wKB/s：每秒发送到设备的写入请求数
%utill：磁盘利用率
剩下的自己看图片

sar命令查看网络的相关统计（sar命令非常复杂，这里仅简单用于统计网络）
sar -n DEV num1 num2
-n，查看网络，DEV表示查看网络接口
num1，刷新间隔（不填默认为1次），num2，刷新次数（不填默认为无限次）
信息解读：
IFACE：本地网卡接口的名称
rxpck/s：每秒接收的数据包
txpck/s：每秒发送的数据包
rxkB/s：每秒接收的数据包大小，单位KB
txkB/s：每秒发送的数据包大小，单位KB
rxcmp/s：每秒接受的压缩包大小
txcmp/s：每秒发送的压缩包大小
rxmcst/s：每秒接受的多播数据包

输入env命令查看当前系统中记录的环境变量
环境变量是一种KeyValue（键值对）型结构，即名称和值

$命令名称取得命令得值（但是直接输出会有问题，所以要用echo）
比如：echo $PWD 或者echo ${PATH}拼接内容

自行设置环境变量：
临时设置：export 变量名=变量值
永久生效：针对当前用户：~/.bashrc文件中
		   针对所有用户：/etc/profile文件中
通过语法：source配置文件，进行立即生效，或者重新登陆生效

rz命令上传、sz命令下载

tar命令压缩和解压
tar [-c -v -x -f -z -C] 参数1 参数2  ......    参数n
-c，创建压缩文件，用于压缩模式
-v，显示压缩、解压过程，用于查看进度
-x，解压模式
-f，要创建的文件，或要解压的文件，-f必须在所有选项中位于最后一个
-z，gzip模式，不使用-z就是普通的tarball格式（-z一般使用的话位于第一个）
-C，选择解压的目的地，用于解压模式（一般单独使用，和其他选项分开cd）
压缩常用组合：
tar -cvf 压缩完的文件 要压缩的文件。。。。
解压常用组合：
tar -xvf 要解压的文件
或者
tar -xvf 要解压的文件 -C 指定解压到的路径

zip命令压缩为zip压缩包
zip [-r] 参数1	参数2  .......   参数n
-r，被压缩的包含文件夹的时候需要使用，和rm、cp等命令的-r效果一致
示例：zip test.zip a.txt b.txt c.txt

unzip命令方便解压zip
unzip [-d] 参数
-d，指定要解压去的路径和tar的-C一样
参数，被解压的包

























