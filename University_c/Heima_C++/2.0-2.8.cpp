#include<stdio.h>
#include <stdlib.h>
#include <iostream>
#include<string>
using namespace std;

int main() {
   
    /*
    //2.2
    short sum1 = 10;
    cout << sizeof(sum1) << endl;
    
    int sum2 = 10;
    cout << sizeof(sum2) << endl;

    //利用sizeof求出数据类型占用内存的大小，一般short，int，long，；onglong分别是2，4，4，8字节
    //一般 short < int <= long <= long long

    //2.3
    /*
    实型（浮点型）
    1.单精度 float 4字节 7位有效数字
    2.双精度 double 8字节 15 - 16位有效数字
    
    

    float f1 = 3.1415926f;
    //加f后才为单精度，不加则默认为双精度
    cout << f1 << endl;

    double d1 = 3.1415926;
    cout << d1 << endl;
    //不管是单精度还是双精度，输出默认为6位有效数字

    cout << sizeof(f1) << endl;
    cout << sizeof(d1) << endl;

    //科学计数法
    float f2 = 3e2;//3 * 10 ^ 2
    cout << f2 << endl;
    float f3 = 3e-2;//3 * 0.1 ^ 2
    cout << f3 << endl;

    //2.4

    //字符型变量的创建方式
    char ch = 'a';
    //只能一个字符
    cout << ch << endl;

    //字符型变量所占内存的大小
    cout << sizeof(ch) << endl;

    //字符型变量常见错误，只能单引号

    //字符型变量对应ASCII编码
    cout << (int)ch << endl;
    //强制转换为int型
    //a--97  A--65

    //2.5

    //转义字符

    //换行符 \\

    cout << "hello\n";

    //反斜杠 \\

    //水平制表符 \t（下一个TAB ，有对齐功能）
    cout << "hkjytig\tkjhg" << endl;
    cout << "jg\tkjhg" << endl;
    cout << "hjg\tkjhg" << endl;

    //2.6

    //字符串型
    //char 变量名[] = "字符串值"
    //string 变量名 = "字符串值"
    //只能用双引号

    char str[] = "hello word\n";
    string strd = "hello word";
    //包含一个头文件 #include <string>
    cout << str << strd << endl;

    //2.7

    //bool型（布尔类型）
    //true和false分别为真和假（本质是1和0）

    //创建bool数据类型

    bool flag = true;
    cout << flag << endl;

    flag = false;
    cout << flag << endl;

    //查看bool类型所占内存空间

    cout << sizeof(bool) << endl;

   

    //2.8
    //数据的输入

    //字符串型
    string str = "hello";
    cout << "请给字符串str赋值：" << endl;
    cin >> str;
    cout << str << endl;


    */
























    system("pause");//按任意键继续 --> 按任意键关闭此窗口

    return 0;//按任意键关闭此窗口
}









// 运行程序: Ctrl + F5 或调试 >“开始执行(不调试)”菜单
// 调试程序: F5 或调试 >“开始调试”菜单

// 入门使用技巧: 
//   1. 使用解决方案资源管理器窗口添加/管理文件
//   2. 使用团队资源管理器窗口连接到源代码管理
//   3. 使用输出窗口查看生成输出和其他消息
//   4. 使用错误列表窗口查看错误
//   5. 转到“项目”>“添加新项”以创建新的代码文件，或转到“项目”>“添加现有项”以将现有代码文件添加到项目
//   6. 将来，若要再次打开此项目，请转到“文件”>“打开”>“项目”并选择 .sln 文件
